snippet NeoBundle
alias   neobundle
abbr NeoBundle ''
    NeoBundle '`getreg('+')=='' ? '<\`0\`>' : getreg('+')`'${0}

snippet NeoBundleLazy
alias   neobundlelazy
abbr NeoBundleLazy ''
    NeoBundleLazy '`getreg('+')=='' ? '<\`0\`>' : getreg('+')`', {
				\   'autoload': {
				\       ${0}
				\   }
				\}

snippet neobundle#tap
alias   tap
options head
	if neobundle#tap('${1}')
		${0}

		call neobundle#untap()
	endif

