---
- name: lo
  hosts: lo
  roles:
    - directory-structure
    - homebrew
    - bash
    - anyenv
    - direnv
    - git
    - vim
    - misc
    - font
  tasks:
    - ansible.builtin.debug:
       var: ansible_facts
